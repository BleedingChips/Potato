export module PotatoArchive;
import std;
import PotatoTMP;
import PotatoMemLayout;


export namespace Potato
{
	/*
	struct Archive
	{
		enum class Type
		{
			File,
			String,
			Binary
		};

		Type GetType() const;

		Archive(Type type, bool is_writing)
			: type(type), is_writing(is_writing) {
		}

	protected:



		Type type;
		bool is_writing;
	};
	*/
}